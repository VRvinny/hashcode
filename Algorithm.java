import Java.util.*;

public class Algorithm {

    // public  cacheQueue = new PriorityQueue();

    public void algorithm() {

        int numberOfVideos = 1;
        int numberOfRequests = 1;
        int numberOfCaches = 1;

        int[] video = new int[numberOfVideos];
        // Read videos

        // cacheID, latency
        ArrayList<HashMap<Integer, Integer>> endpoint = new ArrayList<HashMap<Integer, Integer>>();
        // Read arrayList
        
        String[] request = new String[numberOfRequests];
        // Read requests

        ArrayList<ArrayList<Integer>> cacheQueue = new ArrayList<ArrayList<Integer>>();

        for (int i = 0; i < numberOfCaches; i++) {
            ArrayList<Integer> temp = new ArrayList<Integer>();
            for 


            cacheQueue.add()
            cacheQueue.get(i).sort();
        }

    }


    
}